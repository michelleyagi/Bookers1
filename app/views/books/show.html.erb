<p><%= 'Title:' + @book.title %></p>
<P><%= 'Body:' + @book.body %></P>

<table class="link">
  <tbody>
    <td><%= link_to 'Edit',book_path(@book.id) + "/edit" %></td>
    <td>|</td>
    <td><%= link_to 'Back', "/books" %></td>
  </tbody>
</table>